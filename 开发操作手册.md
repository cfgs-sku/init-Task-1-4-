# 开发操作手册

## 先回答你的三个核心问题

**Q1：每次改完bug推送，需要告诉我在 origin dev 上建立分支吗？**

不需要。`dev` 分支已经建好了，永远存在。你只管改代码，然后双击 `deploy.bat` 选 [1]，或者直接在终端运行：

```
git add .
git commit -m "改动说明"
git push origin dev
```

就这三条命令，每次都一样。

---

**Q2：Cloudflare 是推送 GitHub 后自动部署吗？**

是的，完全自动。流程是：

```
你 git push origin dev
       ↓
GitHub 收到代码（瞬间）
       ↓
Cloudflare Pages 监听到 GitHub 有新提交（自动触发）
       ↓
Cloudflare 拉代码、构建、部署（约30秒~1分钟）
       ↓
https://dev.csfw-purchase.pages.dev 更新完毕
```

你不需要手动操作任何 Cloudflare 的界面。推送完等1分钟，刷新测试地址就能看到新版本。

---

**Q3：每次开发需要给我什么提示词？还是有固定规则？**

已经写入固定规则了（`CLAUDE.md` 文件）。以后你开新对话，我会自动读取这个文件，知道：
- 这个项目是什么
- 分支和数据库怎么隔离
- 文件在哪里
- 开发规范是什么

你只需要直接描述问题，例如：
- "搜索框还是有默认值，帮我修"
- "采购员提交订单后页面没有刷新"
- "新增一个导出Excel功能"

不需要每次解释背景。

---

## 日常开发步骤（完整版）

### 第一步：改代码

在 `src/js/` 或 `src/index.html` 等文件里做修改。

如果你不确定改哪里，直接告诉我问题，我来找文件、做修改。

---

### 第二步：推送到测试环境

双击项目文件夹里的 `deploy.bat`，选 **[1] 推送到测试环境**，输入改动说明。

或者打开命令行（在项目文件夹按 Shift+右键 → 在此处打开终端）：

```bash
git add .
git commit -m "修复：搜索框显示默认值hmq的问题"
git push origin dev
```

等约1分钟，打开 https://dev.csfw-purchase.pages.dev 验证。

---

### 第三步：验证功能

在测试地址正常使用，确认没问题。这个地址用的是测试数据库，不影响真实订单。

---

### 第四步：发布到生产

双击 `deploy.bat`，选 **[2] 测试通过，发布到生产**。

或者手动：

```bash
git checkout main
git merge dev
git push origin main
git checkout dev    # 切回dev，继续开发
```

生产地址 https://csfw-purchase.pages.dev 约1分钟后自动更新。

---

## 查看版本历史

双击 `deploy.bat`，选 **[3] 查看版本历史**。

或者在命令行：

```bash
git log --oneline -10
```

输出示例：
```
a3f2c1b 修复：搜索框默认值问题
8d7e4a2 新增：导出Excel功能
1c2b3d4 修复：订单状态不刷新
```

每行开头的 `a3f2c1b` 就是版本ID，回滚时用。

---

## 回滚操作

### 情况一：刚推了测试，发现有问题，还没发布生产

直接继续修改，再推一次就行，测试环境会覆盖。

### 情况二：已经发布到生产，需要撤回

双击 `deploy.bat`，选 **[4] 回滚生产到指定版本**。

或者手动：

```bash
# 查看历史，找到要回到的版本ID
git log origin/main --oneline -10

# 假设要回到版本 8d7e4a2（在这之后的所有提交都撤销）
git checkout main
git revert 8d7e4a2..HEAD --no-edit
git push origin main
git checkout dev
```

Cloudflare 会在1分钟内把生产回滚到那个版本。

### 情况三：彻底强制重置到某个版本（慎用）

```bash
git checkout main
git reset --hard 8d7e4a2
git push origin main --force
git checkout dev
```

> ⚠️ `--force` 是强制覆盖，会丢失那个版本之后的所有提交记录，慎用。

---

## 在 Cloudflare 界面查看部署历史

1. 打开 https://dash.cloudflare.com
2. 进入 Pages → csfw-purchase
3. 左侧 "Deployments" 可以看到每次部署记录
4. 点击任意一条记录，右上角有 "Rollback to this deployment" 按钮

这是不需要敲命令的图形化回滚方式。

---

## 常见问题

**推送后测试地址没变化？**

等1~2分钟再刷新。如果等了3分钟还没变，去 Cloudflare 仪表盘看 Pages 的构建日志是否报错。

**git push 提示需要登录？**

说明 Git 的远程凭据失效了，重新用 GitHub 账号登录即可：
```
git config --global credential.helper manager
git push origin dev   # 会弹出登录窗口
```

**不小心在 main 分支改了代码怎么办？**

```bash
# 把改动暂存
git stash
# 切回 dev
git checkout dev
# 把改动取出来
git stash pop
# 正常提交推送
git add . && git commit -m "..." && git push origin dev
```

---

## 给我的提示词模板（可选）

虽然不是必须的，但如果你想让我快速定位，可以这样说：

> "【bug】订单提交后，采购员看不到自己的订单，但数据库里有数据"

> "【功能】物资库增加一个按供应商筛选的功能"

> "【优化】移动端搜索框太小，放大一点"

格式不重要，说清楚现象就行。
