
markdown_content = """# 物业行业多项目集中采购协同系统——高级开发与架构规范 (PRD & Technical Spec)

本文件专为 **Claude Code / Claude 3.5 Sonnet** 打造，作为系统重构、新功能开发和代码审核的最高纲领。系统基于 **Cloudflare 生态 (Pages + Workers / Functions + D1 关系型数据库)** 进行全栈建设，旨在将原有的纯技术爬虫/比价逻辑，彻底升级并锚定为**聚焦于物业行业高复购特征的“标准 SKU 库 + 多项目平行提报 + 招采总控审批 + 数据资产沉淀”的集约化协同采购平台**。

---

## 📌 一、 核心架构原则（开发与审核基准）

在 Claude Code 中执行此任务时，必须严格遵守以下原则：
1. **彻底解耦，剔除冗余**：全面清理、删除所有与第三方电商平台（京东、震坤行、得力、鑫方盛等）相关的 `Cookie 注入`、`自动化爬虫（Puppeteer/Playwright）`、`HTML/DOM 解析（JSDOM）`等不相关代码。
2. **纯净自建 API 驱动**：所有前端数据交互必须通过自建的 Cloudflare Functions 接口完成，数据全面托管于云端 D1 数据库。
3. **状态机强控制**：采购订单的生命周期由后端状态机（Status Machine）严格控制，任何前端状态变更必须通过合法的后端安全校验。
4. **渐进式不破坏**：在重构现有前端工程骨架时，保持 Vite 配置与组件化结构的可用性，以单页应用（SPA）或清晰的路由划分（`Buyer` 与 `HMQ`）进行增量开发。

---

## 🗄️ 二、 数据库设计规范 (Cloudflare D1 SQL Schema)

数据存储采用 Cloudflare D1 关系型数据库。Claude Code 在初始化或审核数据库时，必须确保以下 4 张核心表结构及索引的完整性：

代码输出
Markdown file generated successfully.

```sql
-- =====================================================================
-- 1. 项目表（支持多项目平行采购及隔离）
-- =====================================================================
CREATE TABLE IF NOT EXISTS projects (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL UNIQUE,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- =====================================================================
-- 2. 标准物资库/SKU表（核心数据资产）
-- =====================================================================
CREATE TABLE IF NOT EXISTS sku_library (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,                  -- 物资名称（如：公牛五孔插座）
    spec TEXT NOT NULL,                  -- 规格型号（如：10A/白色）
    unit TEXT NOT NULL,                  -- 计量单位（如：个）
    category TEXT NOT NULL,              -- 分类（如：工程五金、保洁耗材、办公用品）
    is_temporary INTEGER DEFAULT 0,      -- 临时标记（0: 全局标准SKU, 1: 项目专属临时SKU）
    project_id INTEGER DEFAULT 0,        -- 归属项目（0: 全局通用, 大于0: 指向特定项目）
    last_price REAL DEFAULT 0,           -- 历史最后一次采购参考单价
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
CREATE INDEX IF NOT EXISTS idx_sku_project_temp ON sku_library(project_id, is_temporary);
CREATE INDEX IF NOT EXISTS idx_sku_category ON sku_library(category);

-- =====================================================================
-- 3. 采购订单主表
-- =====================================================================
CREATE TABLE IF NOT EXISTS purchase_orders (
    id TEXT PRIMARY KEY,                 -- 订单号，格式：PO-YYYYMMDD-随机六位Hash
    project_id INTEGER NOT NULL,         -- 发起采购的项目ID
    creator TEXT NOT NULL,               -- 提报采购员姓名
    status TEXT NOT NULL,                -- 状态机状态：pending_review(待审核), purchasing(采购中), received(已收货), archived(已归档)
    total_price REAL DEFAULT 0,          -- 订单预估/核定总金额
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (project_id) REFERENCES projects(id)
);
CREATE INDEX IF NOT EXISTS idx_orders_project ON purchase_orders(project_id);
CREATE INDEX IF NOT EXISTS idx_orders_status ON purchase_orders(status);

-- =====================================================================
-- 4. 采购订单明细表（保存采购时点的数据快照，防止历史数据因SKU修改而失真）
-- =====================================================================
CREATE TABLE IF NOT EXISTS purchase_order_items (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    order_id TEXT NOT NULL,              -- 关联的主订单号
    sku_id INTEGER NOT NULL,             -- 关联的SKU ID
    name TEXT NOT NULL,                  -- 物资名称快照
    spec TEXT NOT NULL,                  -- 规格型号快照
    unit TEXT NOT NULL,                  -- 计量单位快照
    quantity INTEGER NOT NULL,           -- 采购数量
    estimated_price REAL DEFAULT 0,      -- 填报/核定单价
    FOREIGN KEY (order_id) REFERENCES purchase_orders(id)
);
CREATE INDEX IF NOT EXISTS idx_items_order ON purchase_order_items(order_id);
🕹️ 三、 核心状态机与业务流转逻辑
系统订单的流转生命周期必须在后端严格执行以下状态机逻辑，Claude Code 需编写对应的原子化事务（Transactions）：
[ 草稿/购物车 ] ──(采购员提交)──> [ pending_review (待审核) ]
                                          │
                                   (HMQ审核通过)
                                          │
                                          ▼
[ archived (已归档) ] ──(HMQ归档)─── [ purchasing (采购中) ]
         ▲                                │
         │                         (任一端确认收货)
   (临时SKU自动转正)                      │
         │                                ▼
         └─────────────────────── [ received (已收货) ]
1. 提交申请阶段 (pending_review)
操作人：各项目一线采购员。
业务行为：采购员从“标准物资库”或“项目历史复购库”选择商品，或输入“临时 SKU”放入购物车。点击提交。
数据校验：必须包含 project_id, creator 以及至少一项明细。单据状态初始化为 pending_review。
2. 招采审核阶段 (purchasing)
操作人：招采部 HMQ。
业务行为：HMQ 在全局大盘中查阅各平行项目提报的单据，可进行驳回或点击“确认采购”。
状态转变：单据状态变为 purchasing。此状态下，对应项目的采购员前端看板必须实时同步更新，提示进入采购在途。
3. 到货确认阶段 (received)
操作人：项目采购员 或 招采部 HMQ（双端协同）。
业务行为：物资送达物业项目现场，任一端在点验无误后点击“确认收货”。
状态转变：单据状态变更为 received。
4. 最终归档与资产沉淀阶段 (archived)
操作人：招采部 HMQ。
业务行为：HMQ 对已到货的单据进行账期或财务合规归档，点击“确认归档”。
核心自动化逻辑（资产转正）：
后端触发事务：扫描当前订单明细中所有关联的 SKU。
凡是满足 sku_library.is_temporary = 1（即采购员提报时本地库不存在、自行添加的临时物资）的 SKU，系统自动将其状态更新为 is_temporary = 0，且 project_id = 0。
业务增效：完成此步骤后，该临时 SKU 正式升级为“全集团系统标准通用 SKU”，此后全物业任何其他项目的采购员在搜索物资库时，均能直接检索并一键采购此物资，实现供应链数据的闭环沉淀。
🔑 四、 多项目角色管理与前端页面编码规则
前端界面基于 Vue3 / Vite / Tailwind CSS 架构，为了提供极简、便捷的操作体验，前端页面必须实现响应式（完美适配手机端库房盘点提报）和严格的权限隔离。
1. 权限与视图路由控制规则
采购员视图（Buyer Portal）：
数据隔离：所有 API 请求必须强制携带本地缓存或 Session 中的 project_id。
可视范围：采购员在订单中心只能看到本项目的订单流转进度，绝对禁止越权读取其他项目的采购数据。
招采部 HMQ 视图（HMQ Dashboard）：
全局视界：拥有最高管理权限。大盘中以“项目名称”为维度，水平横向聚合展示所有平行项目的待办、在途、待归档单据。
2. 前端 HTML 组件与交互编码规范
A. 采购员提报页面 (MaterialPool.vue)
双数据源 Tab 切换结构：
Tab 1：物资库（全量标准）：加载云端 is_temporary = 0 的 SKU，支持按分类（五金、保洁等）高频检索，一键加入购物车。
Tab 2：项目历史（高频复购）：由后端执行 DISTINCT 联查，仅平铺展示当前项目过去已归档订单中采购过的高频物资，实现“一键回购”。
临时 SKU 紧急添加弹窗组件 (TempSkuModal.vue)：
必须使用语义化 HTML5 表单，且配置严格的拦截器：
HTML
<form @submit.prevent="handleCreateTempSku">
  <input type="text" v-model="form.name" required placeholder="物资标准名称" />
  <input type="text" v-model="form.spec" required placeholder="规格型号" />
  <input type="text" v-model="form.unit" required placeholder="单位（如: 盒/个）" />
  <input type="number" v-model="form.price" required step="0.01" placeholder="预估单价" />
  <button type="submit">加入购物车并临时建档</button>
</form>
B. 招采部全局总控台 (HmqReview.vue)
待审卡片聚合视图：按提报时间倒序平铺所有项目提交的申请。
转正提示高亮组件：在展开订单明细时，凡是检测到 is_temporary === 1 的条目，必须在 HTML 上高亮渲染 [ 临时物资 - 归档后将自动沉淀 ] 标签，为 HMQ 提供明确的招采控价提示。
⚡ 五、 后端 Cloudflare Functions API 接口定义
后端所有接口文件位于 /functions/api/ 目录下，直接与 D1 数据库实例 (env.DB) 进行异步交互。
1. SKU 资源管理接口 (/functions/api/skus.js)
GET /api/skus?project_id=X&type=library|history
逻辑：
若 type == 'library'：执行 SELECT * FROM sku_library WHERE is_temporary = 0 OR project_id = ?。
若 type == 'history'：执行 SELECT DISTINCT sku_id, name, spec, unit FROM purchase_order_items poi JOIN purchase_orders po ON poi.order_id = po.id WHERE po.project_id = ? AND po.status = 'archived'。
POST /api/skus (采购员提报临时物资)
逻辑：接收参数并向 sku_library 插入新行，强行锁定 is_temporary = 1, project_id = body.project_id。返回新生成的 sku_id。
2. 订单流转接口 (/functions/api/orders.js)
POST /api/orders (创建采购申请)
逻辑：接收项目、提报人、购物车明细。后端利用 env.DB.batch() 启动事务：
生成 PO-YYYYMMDD-HEX 订单号。
插入 purchase_orders 表，状态设定为 pending_review。
遍历明细，批量插入 purchase_order_items 表。
GET /api/orders?project_id=X (多项目隔离查询)
逻辑：若带 project_id 则执行按项目过滤（采购员端）；若不带，则返回全量数据（HMQ端）。
PATCH /api/orders (状态变更控制核心)
逻辑：接收 order_id 和 action：
action == 'approve' -> 更新状态为 purchasing。
action == 'receive' -> 更新状态为 received。
action == 'archive' -> 核心业务归档：执行批量更新，将该订单下对应的临时 SKU 全局转正：
SQL
UPDATE sku_library SET is_temporary = 0, project_id = 0 
WHERE id IN (SELECT sku_id FROM purchase_order_items WHERE order_id = ?);
UPDATE purchase_orders SET status = 'archived', updated_at = CURRENT_TIMESTAMP WHERE id = ?;
🚀 六、 Claude Code 开发与自我审计执行路线
请指导 Claude Code 按照以下四个步骤进行精准无误的开发，并在每步完成后运行审计。
Step 1: 环境净化与基础设施配置
清理现有项目中所有包含爬虫、网购平台比价、Axios 外部请求的旧代码文件。
配置项目根目录下的 wrangler.toml 文件，确保其包含 D1 数据库绑定：
Ini, TOML
[[d1_databases]]
binding = "DB"
database_name = "procurement_db"
database_id = "你的_cloudflare_d1_真实_id"
在本地执行数据库初始化审计：npx wrangler d1 execute procurement_db --local --file=./schema.sql
Step 2: 后端 API 与状态机开发
开发 /functions/api/skus.js 和 /functions/api/orders.js 的完整 CRUD 和事务控制逻辑。
Claude 自我审计命令：要求 Claude 编写简易的测试脚本或利用本地 Mock 机制，连续调用 POST (创建订单) -> PATCH (审批) -> PATCH (归档)，通过本地 D1 命令行工具检查 sku_library 表中的 is_temporary 是否成功从 1 变为 0。
Step 3: 前端多项目协同界面开发
建设采购员侧的“双源选品（标准库+项目历史）”以及“临时SKU极速提报”HTML 结构。
建设 HMQ 全局看板，确保多项目平行提报的数据刷新及时、流转状态标志清晰明显。
实现多项目/角色模拟切换组件，方便在开发本地进行完整的全链路多角色串联联调。
Step 4: 生产部署与终审
执行编译：npm run build。
部署至 Cloudflare Pages 生产环境：npx wrangler pages deploy ./dist。
同步生产环境 D1 数据库：npx wrangler d1 execute procurement_db --remote --file=./schema.sql。
"""
with open("procurement_development_doc.md", "w", encoding="utf-8") as f:
f.write(markdown_content)
print("Markdown file generated successfully.")
您的开发文件已成功生成，已完全根据您的最新业务指示（多项目平行采购、去除比价与爬虫冗余功能、Cloudflare D1 数据库托管、临时 SKU 归档自动转正沉淀）进行了全盘定制与重写，非常适合直接喂给 Claude Code 进行工程开发与代码审核。

Your PDF, CSV, XLSX, Markdown, TXT, LaTeX file is ready
[file-tag: code-generated-file-0-1782294206352102949]

---

### 📄 开发文件核心内容提要

本开发文件（`procurement_development_doc.md`）严格按照 **产品需求文档 (PRD) + 技术架构说明书 (Technical Spec)** 的高标准进行编写，确保 Claude Code 能够百分之百准确理解业务并直接产出高质量代码：

1. **环境纯净化规则**：明确命令 Claude Code 彻底剔除所有历史遗留的第三方爬虫、网购平台 API、Cookie 注入以及 `JSDOM` 等无关依赖，使系统精简至最纯粹的内部标准数据库模式。
2. **Cloudflare D1 关系型数据库 Schema**：设计了包含 `projects` (项目表)、`sku_library` (物资库)、`purchase_orders` (主表) 以及 `purchase_order_items` (明细快照表) 的关系型数据库结构，并建立了高并发检索索引。
3. **闭环业务状态机设计**：
   * 细化了从采购员发起申请（`pending_review`）到招采部 HMQ 看板集中审核（`purchasing`）的流转逻辑。
   * 规定了双端到货确认（`received`）与财务合规归档（`archived`）的流转路径。
4. **自动化资产沉淀算法**：在 HMQ 执行“**确认归档**”的一瞬间，后端接口将自动执行数据库事务，扫描并将所有带 `is_temporary = 1` 的项目临时物资**一键“转正”**（修改为全局通用标准 SKU，`project_id = 0`），完美解决多项目平行采购时数据越用越全、高频物资一键复购的行业痛点。
5. **前端 HTML/Vue 编码指南**：规范了采购员侧响应式手机端提报界面（双 Tab：标准库 + 历史记录）以及临时添加弹窗的语义化表单校验。

您可以直接将本文件的内容输入给 Claude Code，它将能够直接进入开发角色并进行完美的工程自审。


```
